on:
  pull_request:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-fmt:
    runs-on: ubuntu-latest
    name: auto-fmt
    steps:
      - uses: Bogyie/auto-fmt@v0.1.3
